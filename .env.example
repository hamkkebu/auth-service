# ========================================
# Auth Service 환경 변수 설정
# ========================================
#
# 사용 방법:
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. 실제 환경에 맞게 값 수정
# 3. .env 파일은 Git에 커밋하지 마세요!

# ========================================
# 포트 설정 (Auth Service 전용)
# ========================================
# Auth Service: Backend 8081, Frontend 3001, gRPC 9091, MySQL 3307
# Boilerplate: Backend 8080, Frontend 3000, gRPC 9090, MySQL 3306

# Backend HTTP 포트
BACKEND_PORT=8081

# Frontend 개발 서버 포트 (docker-compose)
FRONTEND_PORT=3001

# gRPC 서버 포트
GRPC_SERVER_PORT=9091
GRPC_PORT=9091

# ========================================
# Database Configuration
# ========================================
DB_HOST=localhost
DB_PORT=3307  # Auth Service는 3307 사용
DB_NAME=hamkkebu_auth
MYSQL_ROOT_PASSWORD=root

# ========================================
# Keycloak SSO Configuration (필수)
# ========================================
KEYCLOAK_AUTH_SERVER_URL=http://localhost:8180
KEYCLOAK_REALM=hamkkebu
KEYCLOAK_CLIENT_ID=hamkkebu-backend
KEYCLOAK_CLIENT_SECRET=hamkkebu-backend-secret
KEYCLOAK_ISSUER_URI=http://localhost:8180/realms/hamkkebu
KEYCLOAK_JWK_SET_URI=http://localhost:8180/realms/hamkkebu/protocol/openid-connect/certs

# Keycloak Admin API (사용자 삭제 등)
KEYCLOAK_ADMIN_REALM=master
KEYCLOAK_ADMIN_CLIENT_ID=admin-cli
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# ========================================
# Application Configuration
# ========================================
SPRING_PROFILES_ACTIVE=dev
