version: "3"

services:
  database:
    image: mysql
    container_name: taste-account-service-db
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_HOST=%
      - MYSQL_ROOT_PASSWORD=root
    volumes:
      - ./db/taste/account/data:/var/lib/mysql
    networks:
      - taste-account-service-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: taste-account-service-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://database:3306/
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
    restart: always
    depends_on:
      - database
    links:
      - database
    networks:
      - taste-account-service-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: taste-account-service-frontend
    ports:
      - "80:80"
    restart: always
    depends_on:
      - backend
    links:
      - backend
    networks:
      - taste-account-service-network

networks:
  taste-account-service-network: